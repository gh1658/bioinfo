---
date: 2026-06-05 21:31:47
source: stdin
---
Task: 排查 Tajik 版 PCA 图中 PC1/PC2 解释率异常偏高的问题，并修正绘图和 PCA 输出。

Scripts/commands: 检查 scripts/run_admixture_pca_tree.sh、scripts/plot_admixture_pca_tree.R 与 admixture_pca_tree_tajik/pca.log；运行 plink --bfile admixture_pca_tree_tajik/ld_pruned --pca 200 --out admixture_pca_tree_tajik/pca；运行 Rscript scripts/plot_admixture_pca_tree.R --outdir admixture_pca_tree_tajik --sample-map sampleName_clientId.txt。

Parameters: 当前 Tajik PCA 为 201 个样本，因此全量 PCA 组件数为 N-1=200；解释率使用 200 个 eigenvalue 的总和作为分母。

Outputs: 更新 admixture_pca_tree_tajik/pca.eigenval 和 pca.eigenvec 为 200 个 PC；重绘 pca_PC1_PC2_nature、pca_PC1_PC2_subgroups_nature 和四个亚群局部放大图；修改 scripts/run_admixture_pca_tree.sh 使未来按样本数计算 N-1 个 PC；修改 scripts/plot_admixture_pca_tree.R 使解释率优先使用最长 eigenvalue 文件。

Verification: 原 pca.eigenval 只有 20 行，脚本用前 20 个 eigenvalue 求和导致 PC1=36.38%、PC2=26.23% 是前 20 PC 内部占比；重新计算 200 个 PC 后 pca.eigenval 为 200 行，PC1=23.82%、PC2=17.17%、PC3=11.41%；重绘主图轴标签已显示 PC1 (23.82%) 和 PC2 (17.17%)；bash -n 和 R parse 检查通过。

Troubleshooting: 原因不是物种特异，而是 PCA 解释率分母使用了 PLINK --pca 20 输出的前 20 个 eigenvalue 总和，导致标签被人为放大。

Result: Tajik PCA 图的解释率标签已修正，后续流程默认输出 N-1 个 PC 以避免同类错误。
